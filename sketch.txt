Resource
Generator
DSL


Each element has it's own DSL class


+ Add ablilty to create document without name

Add array data_type

Rename Parameters DSL to something common for request/response (maybe to ApiSketch::DSL::Parameters)

Rename DocumentParser to ComplexAttributeParser

Reuse ApiSketch::DSL::Parameters at response body