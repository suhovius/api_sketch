Resource
Generator
DSL


Each element has it's own DSL class


+ Add ablilty to create document without name

Add array data_type

Rename Parameters DSL to something common for request/response (maybe to ApiSketch::DSL::Attributes) +

Rename DocumentParser to ComplexAttributeParser +

Reuse ApiSketch::DSL::Attributes at response body +

Add model validations: allowed types, values, etc

Add generator for some html site api documentation

Add rack stabs test server


Rename response to responses +

Add params contexts (query, body). It should be keywords +

Add reusable parts of documents, attributes, etc.

Add simple generator for github wiki
